export enum MatchTotalTypes { Less, More }
export enum MatchResultTypes { Draw, WinP1, WinP2 }
export enum MatchTotalValueTypes { HalfAndZero, HalfAndOne, HalfAndTwo }
export enum MatchSatuses { IN_PLAY = 'IN_PLAY', PAUSED = 'PAUSED', FINISHED = 'FINISHED', SCHEDULED = 'SCHEDULED' }
export enum MatchSatusFilterTypes { CURRENT = 1, UPCOMING = 2, FINISHED = 3 }
